<p>Comments (<%= @article.comments.size %>) : </p>

<td>
  <tr>"<%= comment.body %>"</tr>
  <div>
  <tr>Posted by:  <%= comment.author_name %></tr>
  <tr><%= distance_of_time_in_words(comment.created_at, Time.now) %> ago </tr>
  </div>
</td>
